{
  "name": "gare-lotti-milanesi",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "tsc && vite build",
    "deploy": "npm run deploy:cloudflare",
    "deploy:cloudflare": "npm run build && wrangler pages deploy dist --project-name gare-lotti-milanesi --branch main",
    "preview": "vite preview --host 127.0.0.1",
    "test": "vitest run"
  },
  "dependencies": {
    "@vitejs/plugin-react": "^6.0.2",
    "lucide-react": "^1.16.0",
    "react": "^19.2.0",
    "react-dom": "^19.2.0",
    "typescript": "^6.0.3",
    "vite": "^8.0.13"
  },
  "devDependencies": {
    "@types/react": "^19.2.14",
    "@types/react-dom": "^19.2.3",
    "vitest": "^4.1.6",
    "wrangler": "^4.92.0"
  },
  "overrides": {
    "ws": "8.20.1"
  }
}
